- zellijでコピペしたときに g が消えることがある
- simplifyと元を両方残す？元は現実装にないから、勘違いの元になる
- 返事 -> tool -> 報告のstate管理を理解する
- バージインはmotionにも対応しているか？
- 地図にラベルを表示したい
- unitree-sdkをどこまでtoolにできているか調査
- lidarをmid-360に。その場合docking station経由になる
- voice liveの優位性は、LLMとTTSが並列に動いて応答が早いこと。local stt (large-v3)使えるからこれでいいかも
- worktree-llm-bench-v1が未mergeでsmokeもまだ。
- qwen3-30b-a3b-2507のTTFTは十分だけど、streamingなどチューニングの余地はあるかも
- upstream の ImageAnnotations ラッパーが __annotations__ を継承しないバグがある？
- もうひとつupstream bag
	- いえ、原因は upstream にあります。 そこは切り分けておきます。

  バグの所在＝upstream

  根本原因は dimos/agents/mcp/mcp_client.py（upstream由来）の設計です：

  - call_tool（:171-186）が画像をツール結果に入れられない制約のため、プレースホルダ文字列だけ返して画
  像は別メッセージで後追い注入する。
  -
  その結果、プレースホルダのターンでモデルが応答してしまい、まだ新画像が無い状態（＝前の画像が残った
  history）で答える。

  これは observe に限らず画像を返すツール全般で起きる upstream の欠陥です。McpClient
  を素で使えば誰でも踏みます。なので「upstreamは悪くなかった」わけではありません。

  ではなぜ fork だけで直せたか

  「直す場所」が upstream ファイルでなくて済んだ、という話です：

  - このブループリントは fork固有の TimedMcpClient（McpClient のサブクラス）を使っている。
  - 修正に必要だったのが return_direct というツール単位のフラグを立てるだけで、それはサブクラスの
  _mcp_tool_to_langchain オーバーライドから設定できた。

  つまり「upstreamのバグを、fork側のサブクラスで上書き回避した」= fork-local
  なワークアラウンドであって、upstream本体の挙動は依然バグったままです。

  整理すると

  ┌───────────────┬──────────────────────────────────────────────────────────────────────────────┐
  │               │                                     評価                                     │
  ├───────────────┼──────────────────────────────────────────────────────────────────────────────┤
  │ バグの原因    │ upstream（mcp_client.py                                                      │
  │               │ の遅延アーティファクト＋プレースホルダで応答する流れ）                       │
  ├───────────────┼──────────────────────────────────────────────────────────────────────────────┤
  │ observe()     │ 正常（最新フレームを返している）                                             │
  │ 自体          │                                                                              │
  ├───────────────┼──────────────────────────────────────────────────────────────────────────────┤
  │ 今回の修正    │ fork側サブクラスでの回避（upstream無編集）。本筋の修正は upstream を直すこと │
  └───────────────┴──────────────────────────────────────────────────────────────────────────────┘

  CLAUDE.md的に upstream は
  fetch専用／PR禁止なので、「fork内で完結する回避策」を選んだ、という位置づけです。もし将来 upstream
  を直接直したい（あるいは本家へ報告したい）なら、call_tool 側で画像を同一ターンに注入する（Command
  等で単一応答化）のが本質的な直し方になります。
- 物体検知するとき、rerun viewerにbbを表示してほしい
- アーム制御が喫緊の課題になる。go2前提でなくてもいいのでは？
- 物体検出の目的が物の把持なら、vision modelではダメでbounding boxがわかるモデルにしないといけないのでは？
- dimosをdocking stationで動かしたい
- voicevoxをかんたんにrunできるように
- - fork テスト 151/152 が正常に collect。唯一のエラーは pyopenjtalk の C 拡張 × numpy ABI
  不整合で、merge 由来ではなく aarch64/Spark の既存環境問題（memory の pyopenjtalk ABI 記録と一致）
- 自分宛ての発話だけ拾いたい
	1. ウェイクワード
     VADで拾った発話の先頭に呼びかけ語があるか（STT前段 or
     軽量判定）で反応を決める。軽量・誤反応に強い。ハンズフリーを維持しつつ宛先を明示できる。
     2. LLMゲート
     全発話をSTT→LLMに通し、「自分に向けられた発話か」をLLMに判定させ、否なら無言。自然だがコスト/レ
     イテンシ増・誤応答リスクが残る。
     3. マルチモーダル
     カメラの顔/視線方向や人物追従の正面検知、マイクのDOA等で「自分を見て話しているか」を判定。最も自
     然だが重く、実装も大きい。
 - 日本語ぷろんぷとはblueprintかprofileか
 - VLAは服を畳むなど複雑なマニュピレーションをしない限りあまり意味がない
 - ワールドモデルは未来予測
 - sparkで遅延要求満たせているか確認